#include"defines.h"

Procedure listerCommandesEnAttente()
Debut
    FILE *fichier = ouvrir("commandes.txt", "r");
    variables chaine ligneARecuperer[TAILLE_MAX];
    variables entier estLivree = 0, i = 0;

    variables chaine nullTypeDePain[TAILLE_MAX];
    variables entier nullVariable = 0;

    tant que (fgets(ligneARecuperer, TAILLE_MAX, fichier))
    
        i <-- i + 1;
        lire dans fichier(ligneARecuperer,"%s %i %i %i", 
            nullTypeDePain, 
            &nullVariable, 
            &nullVariable, 
            &estLivree
        );

        si (!estLivree)
            ecrire i + ligneARecuperer);
        fin si
        
    fin tant que
    
    fermer(fichier);
fin