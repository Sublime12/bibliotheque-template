inclure"defines.h"

structures Adresse
Debut
    chaine commune[TAILLE_MAX];
    chaine quartier[TAILLE_MAX];
    chaine avenue[TAILLE_MAX];
    entier numero;
Fin

structures Client
Debut
    chaine nom[TAILLE_MAX];
    chaine prenom[TAILLE_MAX];
    chaine sexe;
    chaine numeroDeTelephone[12];
    structures Adresse adresse;
Fin

structures Commande
Debut
    chaine nomDuPain[TAILLE_MAX];
    // entier typeDePain;
    entier prixDePain;
    entier nombreDePain;
    // double prixTotal;
Fin
