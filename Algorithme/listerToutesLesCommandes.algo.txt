#include "defines.h"

Procedure listerToutesLesCommandes()
Debut
    FILE *fichier = fopen("commandes.txt", "r");
    chaine commande[TAILLE_MAX];

    Ecrire "type-de-pain\tprix-unitaire \tquantite-en-fc\n\n" ;

    variable entier i = 0;
    Tant que (fgets(commande, TAILLE_MAX, fichier) != NULL)
        i++;
        // int tailleChaine = strlen(commande);
        // commande[tailleChaine - 2] = ' ';
        // commande[tailleChaine - 3] = ' ';

        Ecrire  i + ") " + commande);
    Fin tant que

Fin